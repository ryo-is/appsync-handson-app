v-container.home
  v-row(justify="center", align-content="center", style="height: calc(100vh - 200px);")
    v-col(cols=4)
      v-text-field(v-model="userID" label="userID")
      v-text-field(v-model="postTitle" label="title")
    v-col(cols=12)
      v-btn(color="indigo" depressed large dark @click="submit()") Submit
